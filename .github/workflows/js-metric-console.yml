name: JavaScript Metrics Console integration tests
on:
  pull_request:
    paths:
      - 'src/js/metrics/console/**'
      - 'test/metrics/**'
      - '.github/workflows/integration-template.yml'
      - '.github/workflows/js-metric-console.yml'
  workflow_dispatch:
jobs:
  integration-tests:
    name: Test
    uses: joaopgrassi/otel-recipes/.github/workflows/integration-template.yml@main
    with:
      sample_name: js.console.app
      sample_path: ./src/js/metrics/console
      telemetry_signal: metrics
