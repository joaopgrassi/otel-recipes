name: Go Metrics Console integration tests
on:
  pull_request:
    paths:
      - 'src/go/metrics/console/**'
      - 'test/metrics/**'
      - '.github/workflows/integration-template.yml'
      - '.github/workflows/go-metric-console.yml'
  workflow_dispatch:
jobs:
  integration-tests:
    name: Test
    uses: joaopgrassi/otel-recipes/.github/workflows/integration-template.yml@main
    with:
      sample_name: go.console.app
      sample_path: ./src/go/metrics/console
      telemetry_signal: metrics
