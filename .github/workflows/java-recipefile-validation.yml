name: Java recipefile JSON schema validation
on:
  pull_request:
    paths:
      - 'otel-recipes-schema.json'
      - 'src/java/recipefile.json'
      - '.github/workflows/jsonschema-validation-template.yml'
      - '.github/workflows/java-recipefile-validation.yml'
  workflow_dispatch:
jobs:
  jsonschema-tests:
    name: Test
    uses: joaopgrassi/otel-recipes/.github/workflows/jsonschema-validation-template.yml@main
    with:
      language: java
