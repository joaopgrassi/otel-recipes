name: Java Metrics Console integration tests
on:
  pull_request:
    paths:
      - 'src/java/metrics/console/**'
      - 'test/metrics/**'
      - '.github/workflows/integration-template.yml'
      - '.github/workflows/java-metric-console.yml'
  workflow_dispatch:
jobs:
  integration-tests:
    name: Test
    uses: joaopgrassi/otel-recipes/.github/workflows/integration-template.yml@main
    with:
      sample_name: java.console.app
      sample_path: ./src/java/metrics/console
      telemetry_signal: metrics
