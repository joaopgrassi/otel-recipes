name: JavaScript recipefile JSON schema validation
on:
  pull_request:
    paths:
      - 'otel-recipes-schema.json'
      - 'src/js/recipefile.json'
      - '.github/workflows/jsonschema-validation-template.yml'
      - '.github/workflows/js-recipefile-validation.yml'
  workflow_dispatch:
jobs:
  jsonschema-tests:
    name: Test
    uses: joaopgrassi/otel-recipes/.github/workflows/jsonschema-validation-template.yml@main
    with:
      lang: js
